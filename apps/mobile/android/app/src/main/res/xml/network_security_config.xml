<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for Lockbox.
  Enforces HTTPS for all connections. Debug builds allow cleartext
  to localhost only (for local Capacitor dev server).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <!-- Allow cleartext to localhost for Capacitor live-reload dev -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
