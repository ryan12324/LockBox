name = "lockbox-web"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Production defaults to the hosted API. Self-hosted builds can override it:
# VITE_API_URL=https://api.example.com bun run build
