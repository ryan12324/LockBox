name = "lockbox-web"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
# Deploy with: wrangler pages deploy dist --project-name=lockbox-web

[env.production]
# Set VITE_API_URL to your Workers API URL at build time:
# VITE_API_URL=https://lockbox-api.your-subdomain.workers.dev bun run build
