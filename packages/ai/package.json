{
  "name": "@lockbox/ai",
  "version": "0.0.1",
  "private": true,
  "description": "AI features for lockbox — health analysis, breach monitoring, semantic search, chat agent",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lockbox/types": "*",
    "@lockbox/crypto": "*",
    "@lockbox/generator": "*"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  }
}
